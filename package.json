{
  "name": "sdvosbnews",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n web,review-api -c blue,green \"npm --prefix web run dev\" \"cd review-interface/backend && python3 -m uvicorn app.main:app --reload --port 8010\"",
    "dev:web": "npm --prefix web run dev",
    "dev:review-api": "cd review-interface/backend && python3 -m uvicorn app.main:app --reload --port 8010",
    "build:web": "npm --prefix web run build",
    "lint:web": "npm --prefix web run lint",
    "test:review-api": "cd review-interface/backend && PYTHONPATH=. python3 -m unittest tests.test_repository"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
