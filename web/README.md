# SDVOSBNews Web App

This app contains:

- the public homepage
- the signal archive
- the newsletter signup page
- the premium offer page
- the internal review dashboard
- the internal ops dashboard

The review dashboard expects the FastAPI backend from `../review-interface/backend` to be running on `http://127.0.0.1:8010/api`.
